<!DOCTYPE html>
<html>
<head>
  <title>Kalendarz Imprez </title>
  <%= csp_meta_tag %>
  <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
<%= javascript_include_tag 'application' %>
<%= csrf_meta_tags %>
</head>
<body class="bg-gray-50">

  <nav class="bg-blue-600 text-white shadow-lg">
    <div class="container mx-auto px-4 py-3 flex justify-between">
      <a href="/" class="text-xl font-bold">Kalendarz Imprez</a>
      <div class="space-x-4">
        <a href="<%= events_path %>" class="hover:underline">Wydarzenia</a>
        <a href="<%= users_path %>" class="hover:underline">Artyści</a>
        <a href="<%= categories_path %>" class="hover:underline">Kategorie</a>
      </div>
    </div>
  </nav>

  <main class="py-8">
    <%= yield %>
  </main>

</body>
</html>
